const cards=[...document.querySelectorAll(".card")];let current=0,startX=0;function show(i){current=(i+cards.length)%cards.length;cards.forEach((card,index)=>card.classList.toggle("active",index===current));cards[current].scrollIntoView({behavior:"smooth",block:"nearest",inline:"center"})}document.querySelector("#next").onclick=()=>show(current+1);document.querySelector("#prev").onclick=()=>show(current-1);const gallery=document.querySelector("#gallery");gallery.addEventListener("pointerdown",e=>startX=e.clientX);gallery.addEventListener("pointerup",e=>{if(Math.abs(e.clientX-startX)>35)show(e.clientX<startX?current+1:current-1)});show(0);